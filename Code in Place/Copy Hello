#include <stdio.h>
#include <string.h>

int main() 
{
    char str1[] = "Hello"; //no need to specify size
    char str2[20]; //empty string must have size

    strcpy(str2, str1);  //copy str1(src) into str2(dest)
    printf("Copied string is: %s\n", str2);
    return 0;
}
